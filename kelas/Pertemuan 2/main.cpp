#include <iostream>
using namespace std;

int main() {
    // string nama[5] = {"Dimas", " Harahap", "Santoso", "Kalingga"};

    // int angka[3];
    // angka[0] = 1;
    // angka[1] = 2;
    // angka[2] = 3;

    // cout << nama[1] << endl;
    // cout << angka[1] << endl;

    // cout << angka << endl;

    // int panjang2 = sizeof(angka) / sizeof(angka[0]);
    // cout << "panjang1\t" << panjang1 << endl;
    // cout << "panjang2\t" << panjang2 << endl;

    // cout << sizeof(int) << endl;
    // cout << sizeof(bool) << endl;
    // cout << sizeof(double) << endl;
    // cout << sizeof(string) << endl;

    // int angka[5] = {1, 2, 3, 4, 5};
    // int panjang1 = sizeof(angka) / sizeof(int);

    // for (int i = 100; i < panjang1; i++) {
    //     cout << angka[i] << endl;
        // }
        
//     for (int item : angka) {
//         cout << item << endl;
//         }

    // int angka[5] = {1, 2, 3, 4, 5};
    // int panjang1 = sizeof(angka) / sizeof(int);
    // for (int i=0; i <panjang1; i++)

    int angka[5] = {1, 2, 3, 4, 5};
    int panjang1 = sizeof(angka) / sizeof(angka[0]);
    
    for (int i = 0 < panjang1; i++);
        cout <, angka[1] << endl;
    return 0;
}